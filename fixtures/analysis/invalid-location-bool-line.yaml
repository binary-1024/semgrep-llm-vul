schema_version: 1
mode: known_sink
target:
  repo_url: https://github.com/example/flask-app
  affected_version: v1.0.0
description: |
  Location uses a boolean where an integer is required.
sink:
  signature: flask.redirect(location)
  location:
    path: app/routes.py
    start_line: true
