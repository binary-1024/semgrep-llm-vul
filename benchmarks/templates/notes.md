# replace-with-kebab-case-id

## 背景

说明这个 case 来自哪里、为什么选择它，以及它要验证哪一个能力边界。

## 来源

- 来源类型：`curated_minimal`、`synthetic_benchmark` 或 `real_vulnerability`
- 原始来源：URL 或 `project-curated`
- fixture 类型：真实输出、真实输出裁剪版、公开 patch 裁剪版、项目自写 distillation 或合成近似结构
- 许可证：说明上游许可证或写 `project`

## 关键证据

- 输入 artifact：`fixtures/...`
- 期望输出：说明应生成或不应生成哪些 candidate。

## 当前验证边界

说明当前只验证 M1/M2/M3/M4 的哪一部分，不要把 candidate 误写成 verified。

## 已知失败模式

列出这个 case 主要防止哪类误报、漏报或结构错误。

## 后续升级

说明它是否可以升级到 taint path、PoC 或 exp 验证。
